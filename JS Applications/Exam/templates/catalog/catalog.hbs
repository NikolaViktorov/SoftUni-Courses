<div class=" mt-3 ">
    <div class="row d-flex d-wrap">
        <div class="card-deck d-flex justify-content-center">
            {{#each movies}}
                {{>movie}}

            {{else}}
                <h1>No movies...</h1>
            {{/each}}
        </div>
    </div>
</div>